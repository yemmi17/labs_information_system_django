# -*- coding: utf-8 -*-
# ===== Служебный комментарий модуля ==========================================
# Модуль: employees.apps
# Назначение: конфигурация Django-приложения employees и подключение сигналов.
# Исполнитель изменения: Yemmi
# Дата изменения: 02.05.2026
# Причина изменения: документировать жизненный цикл приложения.
# Первоначальный фрагмент: стандартный AppConfig с методом ready().
# =============================================================================

from django.apps import AppConfig


class EmployeesConfig(AppConfig):
    """Конфигурация приложения employees.

    Тип: класс конфигурации приложения Django.
    Формальные данные: настройки класса AppConfig.
    Фактические данные: Django вызывает конфигурацию при загрузке проекта.
    Условия: путь employees.apps.EmployeesConfig должен быть в INSTALLED_APPS.
    Результат: приложение регистрируется и подключает обработчики сигналов.
    """

    # Тип автоматического первичного ключа для моделей приложения.
    default_auto_field = 'django.db.models.BigAutoField'
    # Системное имя приложения внутри Django.
    name = 'employees'

    def ready(self):
        """Подключает модуль signals, когда приложение готово.

        Тип подпрограммы: метод жизненного цикла AppConfig.
        Формальные параметры: self содержит текущую конфигурацию приложения.
        Фактические данные: модуль employees.signals с обработчиками post_migrate.
        Условия: импорт выполняется после загрузки реестра приложений Django.
        Возвращает: None.
        """
        # Импорт нужен ради регистрации receiver-функций, поэтому имя signals не используется напрямую.
        from . import signals  # noqa: F401
